--- client/src/components/AiSuggestButton.tsx
+++ client/src/components/AiSuggestButton.tsx
@@ -56,6 +56,7 @@
           className={className}
           data-testid={`button-ai-suggest-${field}`}
           aria-label={`Generate ${field.replace(/-/g, ' ')} suggestion`}
+          title={`Generate ${field.replace(/-/g, ' ')} suggestion`}
           aria-busy={isLoading}
         >
           {isLoading ? (
